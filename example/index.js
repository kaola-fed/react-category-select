const React = require('react');
const ReactDOM = require('react-dom');
const CategorySelect = require('../index');

require('../assets/index.css');

const data = [];

ReactDOM.render(<CategorySelect />, document.getElementById('app'));